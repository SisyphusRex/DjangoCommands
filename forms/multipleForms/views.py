def listing(request, listing_id):
  # lots of stuff here but not related to form

  if request.method = "POST":
    # here we are looking for the names we assigned to the html form input buttons
    if "bid" in request.POST:
      # do bid logic
      elif "watch" in request.POST:
        # do watch logic
